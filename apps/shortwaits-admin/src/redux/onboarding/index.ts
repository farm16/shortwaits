// import { createSlice, PayloadAction } from "@reduxjs/toolkit"
// import { BusinessType } from "@shortwaits/shared-types"

// import type { RootState } from "../types"

// const slice = createSlice({
//   name: "business_onboarding",
//   initialState: null as Partial<BusinessType> | null,
//   reducers: {
//     setOnboarding: (state, action: PayloadAction<Partial<BusinessType>>) => ({
//       ...state,
//       ...action.payload
//     }),
//     clearOnboarding: () => null
//   }
// })

// export const { setOnboarding } = slice.actions

// export default slice.reducer

// export const selectCurrentOnboardingState = (state: RootState) =>
//   state.onboarding
